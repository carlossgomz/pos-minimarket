node_modules
dist
src-tauri/target
src-tauri/gen
*.db
*.db-journal
.DS_Store

# Clave privada de firma del updater — nunca se commitea, ver el plan de
# la sesión (Tauri updater + GitHub Releases). La pública sí va en
# tauri.conf.json.
*.key
*.key.pub.bak

.claude/
